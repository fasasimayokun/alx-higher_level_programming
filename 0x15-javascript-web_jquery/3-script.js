// a JavaScript script that adds the class red to the <header> element when
// the user clicks on the tag DIV#red_header, You must use the JQuery API
$('DIV#red_header').click(function () {
  $('HEADER').addClass('red');
});
